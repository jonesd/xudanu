# Environment file for production deployment
# Copy to .env and fill in values

# Passphrase for server key encryption (generate a strong random one)
# Example: openssl rand -base64 32
XUDANU_KEY_PASSPHRASE=CHANGE_ME_TO_A_STRONG_RANDOM_STRING
