[package]
name = "ent-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ent-core = { path = "../ent-core" }
ent-api-types = { path = "../ent-api-types" }

[dev-dependencies]
tower = "0.5"
http = "1"

