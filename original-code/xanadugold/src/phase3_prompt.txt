We are about to begin Phase 3, but implementation must not start yet.

Provide a semantic explanation of ordering in the Ent/DagWood model.

Specifically:

1. What does it mean for one TracePosition to be <= another?

   * Is this ancestry in a tree?
   * reachability in a DAG?
   * something else?

2. What evidence from the original code supports this interpretation?

3. How does ordering relate to:

   * Branch structure
   * trunk map
   * future DagBranch (merge) behavior

4. Why does the navigation cache exist?

   * what problem does it solve?
   * what would break without it?

5. What is the minimal correct behavior needed for Phase 3?

6. What are the risks if ordering is implemented incorrectly?

Do not propose code.
Do not propose data structures yet.
Focus only on semantics and evidence.

